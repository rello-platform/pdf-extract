{
  "name": "@rello-platform/pdf-extract",
  "version": "0.2.0",
  "description": "Deterministic PDF coordinate primitives for the Rello platform — promoted from PathfinderPro's grid-extract so PFP (rate sheets) and Rello (Closing Disclosure reconciliation, Wave Milo-CD Phase 1) consume one bulletproof core. Pure geometry; NO model calls; NO Anthropic key holder (Kelly lock 2026-05-21: Anthropic keys live only in Milo Engine).",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "compile": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "prepublishOnly": "npm run compile"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rello-platform/pdf-extract.git"
  },
  "publishConfig": {
    "registry": "https://npm.pkg.github.com"
  },
  "license": "UNLICENSED",
  "dependencies": {
    "pdfjs-dist": "^5.7.284"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.9.3",
    "vitest": "^4.1.6"
  }
}
